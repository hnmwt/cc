# フロントエンド要件定義書

**作成日**: 2025-11-21
**対象**: 外観検査アプリケーション Webフロントエンド
**バックエンド**: C++ REST API (inspection-server)
**ステータス**: 要件定義中

---

## 📋 目次

1. [現状分析](#現状分析)
2. [フロントエンド要件](#フロントエンド要件)
3. [技術スタック比較](#技術スタック比較)
4. [機能要件](#機能要件)
5. [UI/UXデザイン](#uiuxデザイン)
6. [API統合仕様](#api統合仕様)
7. [実装計画](#実装計画)
8. [コスト・工数見積](#コスト工数見積)

---

## 現状分析

### バックエンドAPI仕様

| エンドポイント | メソッド | 機能 | レスポンス |
|--------------|---------|------|-----------|
| `GET /` | GET | サーバー情報 | JSON |
| `POST /api/v1/inspect` | POST | 検査実行 | JSON (結果) |
| `GET /api/v1/status` | GET | ステータス取得 | JSON |
| `GET /api/v1/statistics` | GET | 統計情報 | JSON |
| `GET /api/v1/detectors` | GET | 検出器一覧 | JSON |
| `POST /api/v1/config` | POST | 設定更新 | JSON |

### バックエンドの機能

- ✅ **4種類の検出器**
  - TemplateMatcher: テンプレート不一致
  - FeatureDetector: 特徴点異常
  - BlobDetector: 塊状欠陥（汚れ、異物）
  - EdgeDetector: 線状欠陥（傷、クラック）

- ✅ **データ出力**
  - CSV出力（Excel対応）
  - 画像保存（元画像/処理済み/可視化）

- ✅ **外部連携**
  - REST API (HTTP:8080)
  - TCP トリガー (9000)
  - CORS有効化済み

### 不足している機能

- ❌ **画像アップロードAPI**
- ❌ **リアルタイム通知**（WebSocket）
- ❌ **ユーザー認証・認可**
- ❌ **検査履歴の永続化**（現在はメモリのみ）

---

## フロントエンド要件

### 1. 必須機能

#### 1.1 画像管理
- **画像アップロード**
  - ドラッグ&ドロップ対応
  - 複数ファイル一括アップロード
  - プレビュー表示
  - 対応形式: JPEG, PNG, BMP

- **画像ライブラリ**
  - アップロード済み画像一覧
  - サムネイル表示
  - 検索・フィルタリング
  - ページネーション

#### 1.2 検査実行
- **検査開始**
  - ワンクリック検査
  - 検出器選択（複数選択可）
  - パラメータ設定
  - バッチ処理

- **進捗表示**
  - プログレスバー
  - リアルタイム進捗
  - キャンセル機能

#### 1.3 結果表示
- **検査結果ビュー**
  - OK/NG判定表示
  - 欠陥数カウント
  - 処理時間表示
  - 信頼度スコア

- **欠陥可視化**
  - 画像上にバウンディングボックス表示
  - 欠陥タイプ別の色分け
  - ズーム・パン機能
  - 欠陥詳細情報（クリックで表示）

- **比較表示**
  - 元画像 / 処理済み画像の並列表示
  - スライダーでの比較
  - 差分表示

#### 1.4 統計ダッシュボード
- **KPI表示**
  - 総検査数
  - OK/NG率
  - 平均処理時間
  - 欠陥タイプ別統計

- **グラフ・チャート**
  - 時系列グラフ（検査数推移）
  - 円グラフ（欠陥タイプ分布）
  - 棒グラフ（検出器別統計）
  - ヒートマップ（欠陥発生位置）

#### 1.5 設定管理
- **検出器設定**
  - 検出器ON/OFF
  - パラメータ調整（スライダー）
  - プリセット保存/読込
  - リアルタイムプレビュー

- **システム設定**
  - 信頼度閾値
  - ログレベル
  - 通知設定

### 2. 推奨機能

#### 2.1 ユーザー管理
- ログイン/ログアウト
- ユーザー権限管理（管理者/オペレーター）
- アクティビティログ

#### 2.2 レポート機能
- PDF出力
- Excel出力
- 検査レポート自動生成
- カスタムレポートテンプレート

#### 2.3 リアルタイム機能
- WebSocketによるリアルタイム通知
- ライブ検査モニタリング
- 複数ユーザー同時接続

#### 2.4 高度な機能
- 機械学習モデル管理
- A/Bテスト機能
- APIドキュメント（Swagger UI）

---

## 技術スタック比較

### フロントエンドフレームワーク

#### 1. React + TypeScript（推奨）

**メリット:**
- ✅ 業界標準、豊富なエコシステム
- ✅ TypeScriptで型安全性
- ✅ コンポーネント再利用性高い
- ✅ React Hooksで状態管理が容易
- ✅ Next.jsでSSR/SSG対応可能

**デメリット:**
- ⚠️ 学習曲線やや急
- ⚠️ ボイラープレートコード多め

**推奨ライブラリ:**
```json
{
  "react": "^18.2.0",
  "typescript": "^5.0.0",
  "vite": "^5.0.0",
  "react-router-dom": "^6.20.0",
  "react-query": "^5.0.0",
  "zustand": "^4.4.0",
  "tailwindcss": "^3.3.0",
  "shadcn/ui": "latest",
  "recharts": "^2.10.0",
  "react-dropzone": "^14.2.0",
  "axios": "^1.6.0"
}
```

**プロジェクト構成:**
```
frontend/
├── src/
│   ├── components/      # UIコンポーネント
│   ├── pages/          # ページコンポーネント
│   ├── api/            # API クライアント
│   ├── hooks/          # カスタムフック
│   ├── store/          # 状態管理
│   ├── types/          # TypeScript型定義
│   ├── utils/          # ユーティリティ
│   └── App.tsx
├── public/
├── package.json
└── vite.config.ts
```

---

#### 2. Vue.js + TypeScript

**メリット:**
- ✅ 学習曲線が緩やか
- ✅ 公式ライブラリが充実（Vue Router, Pinia）
- ✅ テンプレート構文が直感的
- ✅ リアクティブシステムが強力

**デメリット:**
- ⚠️ エコシステムがReactより小さい
- ⚠️ TypeScript対応がやや弱い

**推奨ライブラリ:**
```json
{
  "vue": "^3.3.0",
  "typescript": "^5.0.0",
  "vite": "^5.0.0",
  "vue-router": "^4.2.0",
  "pinia": "^2.1.0",
  "naive-ui": "^2.35.0",
  "echarts": "^5.4.0",
  "axios": "^1.6.0"
}
```

---

#### 3. Next.js（React + SSR）

**メリット:**
- ✅ SEO対応
- ✅ サーバーサイドレンダリング
- ✅ APIルート機能（バックエンド不要の場合）
- ✅ 画像最適化

**デメリット:**
- ⚠️ オーバースペック（この用途には）
- ⚠️ ホスティングコスト高め

---

### UIライブラリ比較

| ライブラリ | 特徴 | コンポーネント数 | カスタマイズ性 |
|-----------|------|----------------|--------------|
| **shadcn/ui** | Tailwind CSS、コピー&ペースト | 50+ | ★★★★★ |
| **Material-UI** | Google Material Design | 100+ | ★★★☆☆ |
| **Ant Design** | エンタープライズ向け、豊富 | 80+ | ★★★★☆ |
| **Chakra UI** | アクセシビリティ重視 | 60+ | ★★★★☆ |
| **Naive UI** | Vue専用、軽量 | 80+ | ★★★★☆ |

**推奨: shadcn/ui + Tailwind CSS**
- 完全なカスタマイズ性
- モダンなデザイン
- TypeScript完全対応
- 軽量

---

### 状態管理

| ライブラリ | 学習曲線 | パフォーマンス | 推奨用途 |
|-----------|---------|--------------|---------|
| **Zustand** | 低 | ★★★★★ | 小〜中規模 |
| **Redux Toolkit** | 中 | ★★★★☆ | 大規模 |
| **Jotai** | 低 | ★★★★★ | 小〜中規模 |
| **React Query** | 低 | ★★★★★ | サーバー状態 |

**推奨: Zustand + React Query**
- Zustand: アプリケーション状態
- React Query: サーバー状態（API）

---

### グラフライブラリ

| ライブラリ | グラフ種類 | パフォーマンス | カスタマイズ性 |
|-----------|-----------|--------------|--------------|
| **Recharts** | 8種類 | ★★★★☆ | ★★★★☆ |
| **Chart.js** | 10種類 | ★★★★★ | ★★★☆☆ |
| **ECharts** | 20種類+ | ★★★★★ | ★★★★★ |
| **Victory** | 10種類 | ★★★☆☆ | ★★★★★ |

**推奨: Recharts**
- React専用
- TypeScript対応
- シンプルで使いやすい

---

## 機能要件

### 画面一覧

#### 1. ダッシュボード (/)
```
┌─────────────────────────────────────────┐
│  🏠 ダッシュボード                       │
├─────────────────────────────────────────┤
│  KPIカード                              │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐   │
│  │ 総検査 │ │ OK率 │ │ NG率 │ │ 平均時間│   │
│  │ 1,234 │ │ 95.4%│ │ 4.6% │ │ 45ms │   │
│  └──────┘ └──────┘ └──────┘ └──────┘   │
│                                         │
│  グラフエリア                            │
│  ┌─────────────────┐ ┌───────────────┐  │
│  │ 検査数推移       │ │ 欠陥タイプ分布 │  │
│  │ (折れ線グラフ)   │ │ (円グラフ)    │  │
│  └─────────────────┘ └───────────────┘  │
│                                         │
│  最近の検査                              │
│  ┌─────────────────────────────────┐    │
│  │ 画像  | 結果 | 時刻 | 欠陥数    │    │
│  └─────────────────────────────────┘    │
└─────────────────────────────────────────┘
```

#### 2. 検査実行画面 (/inspect)
```
┌─────────────────────────────────────────┐
│  🔍 検査実行                             │
├─────────────────────────────────────────┤
│  画像アップロード                        │
│  ┌─────────────────────────────────┐    │
│  │ 📁 ドラッグ&ドロップ または クリック │    │
│  │                                 │    │
│  └─────────────────────────────────┘    │
│                                         │
│  検出器選択                              │
│  ☑ BlobDetector   ☑ EdgeDetector       │
│  ☐ FeatureDetector ☐ TemplateMatcher   │
│                                         │
│  パラメータ調整                          │
│  信頼度閾値: [====|===] 0.5             │
│                                         │
│  [  検査開始  ]                          │
└─────────────────────────────────────────┘
```

#### 3. 結果表示画面 (/results/:id)
```
┌─────────────────────────────────────────┐
│  📊 検査結果                             │
├─────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐       │
│  │ 元画像       │  │ 検査結果     │       │
│  │             │  │   NG (2件)  │       │
│  │             │  │ ┌─────────┐ │       │
│  │             │  │ │🔴 欠陥1  │ │       │
│  │             │  │ │🔴 欠陥2  │ │       │
│  │             │  │ └─────────┘ │       │
│  └─────────────┘  └─────────────┘       │
│                                         │
│  欠陥詳細                                │
│  ┌─────────────────────────────────┐    │
│  │ タイプ: 傷 | 信頼度: 0.85       │    │
│  │ 位置: (120, 340) | 面積: 450px²│    │
│  └─────────────────────────────────┘    │
│                                         │
│  [CSV出力] [PDF出力] [再検査]           │
└─────────────────────────────────────────┘
```

#### 4. 設定画面 (/settings)
```
┌─────────────────────────────────────────┐
│  ⚙️ 設定                                 │
├─────────────────────────────────────────┤
│  検出器設定                              │
│  ┌─ BlobDetector ───────────────────┐   │
│  │ 有効: ☑                          │   │
│  │ 最小面積: [====|] 50             │   │
│  │ 円形度: [==|====] 0.3            │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ┌─ EdgeDetector ───────────────────┐   │
│  │ 有効: ☑                          │   │
│  │ Canny低閾値: [==|====] 50        │   │
│  │ Canny高閾値: [====|==] 150       │   │
│  └─────────────────────────────────┘   │
│                                         │
│  [設定保存] [デフォルトに戻す]           │
└─────────────────────────────────────────┘
```

#### 5. 履歴画面 (/history)
```
┌─────────────────────────────────────────┐
│  📜 検査履歴                             │
├─────────────────────────────────────────┤
│  フィルター: [すべて ▼] [日付範囲 ▼]     │
│  検索: [____________] 🔍                │
│                                         │
│  ┌─────────────────────────────────┐    │
│  │ 画像  | 結果 | 日時 | 欠陥数  | 詳細│    │
│  ├─────────────────────────────────┤    │
│  │ [img] | NG  | 13:45| 2      | 👁  │    │
│  │ [img] | OK  | 13:40| 0      | 👁  │    │
│  │ [img] | NG  | 13:35| 1      | 👁  │    │
│  └─────────────────────────────────┘    │
│  ページ: [1] 2 3 ... 10                 │
└─────────────────────────────────────────┘
```

---

## API統合仕様

### APIクライアント設計

```typescript
// api/client.ts
import axios from 'axios';

const API_BASE_URL = process.env.VITE_API_URL || 'http://localhost:8080';

export const apiClient = axios.create({
  baseURL: API_BASE_URL,
  timeout: 30000,
  headers: {
    'Content-Type': 'application/json',
  },
});

// インターセプター（認証トークン追加）
apiClient.interceptors.request.use((config) => {
  const token = localStorage.getItem('authToken');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

// エラーハンドリング
apiClient.interceptors.response.use(
  (response) => response,
  (error) => {
    if (error.response?.status === 401) {
      // 認証エラー → ログイン画面へ
      window.location.href = '/login';
    }
    return Promise.reject(error);
  }
);
```

### API関数定義

```typescript
// api/inspection.ts
export interface InspectionRequest {
  image_path: string;
  detectors?: string[];
  parameters?: Record<string, any>;
}

export interface Defect {
  type: string;
  bbox: { x: number; y: number; width: number; height: number };
  confidence: number;
  center: { x: number; y: number };
  area: number;
}

export interface InspectionResult {
  result: 'OK' | 'NG';
  defect_count: number;
  defects: Defect[];
  processing_time_ms: number;
  timestamp: string;
}

// 検査実行
export const runInspection = async (
  request: InspectionRequest
): Promise<InspectionResult> => {
  const { data } = await apiClient.post('/api/v1/inspect', request);
  return data;
};

// 統計情報取得
export const getStatistics = async () => {
  const { data } = await apiClient.get('/api/v1/statistics');
  return data;
};

// 検出器一覧取得
export const getDetectors = async () => {
  const { data } = await apiClient.get('/api/v1/detectors');
  return data;
};
```

### React Query統合

```typescript
// hooks/useInspection.ts
import { useMutation, useQuery } from '@tanstack/react-query';
import { runInspection, getStatistics } from '../api/inspection';

export const useRunInspection = () => {
  return useMutation({
    mutationFn: runInspection,
    onSuccess: (data) => {
      console.log('Inspection completed:', data);
    },
    onError: (error) => {
      console.error('Inspection failed:', error);
    },
  });
};

export const useStatistics = () => {
  return useQuery({
    queryKey: ['statistics'],
    queryFn: getStatistics,
    refetchInterval: 5000, // 5秒ごとに自動更新
  });
};
```

---

## 実装計画

### Phase 1: プロジェクトセットアップ（1日）

**タスク:**
1. Vite + React + TypeScript プロジェクト作成
2. 必要なライブラリインストール
3. ディレクトリ構成設定
4. ESLint/Prettier設定
5. Git初期化

**コマンド:**
```bash
# プロジェクト作成
npm create vite@latest frontend -- --template react-ts

# 依存関係インストール
cd frontend
npm install react-router-dom @tanstack/react-query zustand
npm install axios recharts
npm install -D tailwindcss postcss autoprefixer
npm install -D @types/node

# Tailwind CSS初期化
npx tailwindcss init -p
```

---

### Phase 2: 基本レイアウト（2日）

**タスク:**
1. ヘッダー/サイドバー/フッターコンポーネント
2. ルーティング設定
3. レスポンシブデザイン
4. ダークモード対応

**成果物:**
- `components/Layout.tsx`
- `components/Header.tsx`
- `components/Sidebar.tsx`
- `App.tsx`（ルーティング）

---

### Phase 3: API統合（2日）

**タスク:**
1. APIクライアント実装
2. TypeScript型定義
3. React Query セットアップ
4. エラーハンドリング

**成果物:**
- `api/client.ts`
- `api/inspection.ts`
- `types/inspection.ts`
- `hooks/useInspection.ts`

---

### Phase 4: 画像アップロード機能（3日）

**タスク:**
1. ドラッグ&ドロップUI
2. 画像プレビュー
3. バックエンドへのアップロード（要実装）
4. 進捗表示

**成果物:**
- `components/ImageUpload.tsx`
- `pages/InspectPage.tsx`

---

### Phase 5: 検査結果表示（3日）

**タスク:**
1. 画像表示コンポーネント
2. バウンディングボックス描画
3. 欠陥リスト表示
4. 詳細モーダル

**成果物:**
- `components/ImageViewer.tsx`
- `components/DefectList.tsx`
- `pages/ResultPage.tsx`

---

### Phase 6: ダッシュボード（3日）

**タスク:**
1. KPIカード
2. グラフコンポーネント（Recharts）
3. 統計データ取得・表示
4. リアルタイム更新

**成果物:**
- `components/KPICard.tsx`
- `components/ChartPanel.tsx`
- `pages/DashboardPage.tsx`

---

### Phase 7: 設定画面（2日）

**タスク:**
1. フォームコンポーネント
2. パラメータ調整UI
3. 設定保存/読込
4. バリデーション

**成果物:**
- `components/SettingsForm.tsx`
- `pages/SettingsPage.tsx`

---

### Phase 8: テスト・デバッグ（2日）

**タスク:**
1. 単体テスト（Vitest）
2. E2Eテスト（Playwright）
3. バグ修正
4. パフォーマンス最適化

---

### Phase 9: デプロイ（1日）

**タスク:**
1. ビルド設定
2. 環境変数設定
3. Vercel/Netlifyデプロイ
4. ドメイン設定

---

## コスト・工数見積

### 開発工数

| Phase | タスク | 工数 |
|-------|--------|------|
| Phase 1 | セットアップ | 1日 |
| Phase 2 | 基本レイアウト | 2日 |
| Phase 3 | API統合 | 2日 |
| Phase 4 | 画像アップロード | 3日 |
| Phase 5 | 結果表示 | 3日 |
| Phase 6 | ダッシュボード | 3日 |
| Phase 7 | 設定画面 | 2日 |
| Phase 8 | テスト | 2日 |
| Phase 9 | デプロイ | 1日 |
| **合計** | - | **19日** |

### ホスティングコスト

| サービス | 無料枠 | 有料プラン |
|---------|-------|-----------|
| **Vercel** | 100GB/月 | $20/月〜 |
| **Netlify** | 100GB/月 | $19/月〜 |
| **AWS S3 + CloudFront** | - | $5-10/月 |
| **GitHub Pages** | 無料 | - |

**推奨: Vercel（無料枠で十分）**

---

## 次のステップ

### 即座に実行可能

1. **プロジェクト作成**
```bash
npm create vite@latest frontend -- --template react-ts
cd frontend
npm install
```

2. **開発サーバー起動**
```bash
npm run dev
```

3. **バックエンド接続テスト**
```typescript
fetch('http://localhost:8080/api/v1/statistics')
  .then(res => res.json())
  .then(data => console.log(data));
```

---

## 推奨実装順序

```
Step 1: プロジェクトセットアップ
  ↓
Step 2: API統合（最優先）
  ↓
Step 3: 画像アップロード
  ↓
Step 4: 検査結果表示
  ↓
Step 5: ダッシュボード
  ↓
Step 6: 設定画面
  ↓
Step 7: テスト・デプロイ
```

---

## バックエンド側の追加実装

### 必要なAPI拡張

#### 1. 画像アップロードエンドポイント

```cpp
// 新規エンドポイント
POST /api/v1/upload
Content-Type: multipart/form-data

// レスポンス
{
  "image_id": "abc123",
  "image_path": "/app/data/input/abc123.jpg",
  "uploaded_at": "2025-11-21T12:00:00Z"
}
```

#### 2. 画像取得エンドポイント

```cpp
GET /api/v1/images/{image_id}

// レスポンス（画像バイナリまたは署名付きURL）
```

#### 3. 検査履歴エンドポイント

```cpp
GET /api/v1/inspections?page=1&limit=20

// レスポンス
{
  "total": 100,
  "page": 1,
  "limit": 20,
  "inspections": [...]
}
```

---

**作成者**: Claude Code
**最終更新**: 2025-11-21
